/** 自动记忆判断：根据本轮对话决定是否写入长期记忆（OpenClaw 式，无需用户说「记住」）；后台执行，不反馈给用户。 */
export const MEMORY_CONSIDER_SYSTEM_PROMPT = `你是记忆判断助手。根据「用户」与「助手」的一轮对话，判断是否有值得长期记住的**新**内容。

需要记住的包括：用户明确或隐含的偏好（如写作风格、习惯）、重要决定（如选择、约定）、关键事实（如项目名、截止日期、人名与关系）。不需要记住：寒暄、一次性问答、已完成的临时任务描述。

**重要**：若助手只是在复述或回忆已有信息（例如用户问「我叫什么」「我的名字是什么」「你记得什么」、助手仅回答名字或已有记忆），没有新的偏好/决定/事实被表达，则输出 NONE，避免重复存储。

仅输出以下之一（不要其他文字）：
1. 若无任何需要记住的，只输出一行：NONE
2. 若有，先输出一行类型（仅一词）：PREFERENCE、DECISION 或 FACT，换行后输出一行要保存的原文或概括（单行，不要换行）。`;

/** 从对话中抽取「用户希望主脑长期遵守的规则/偏好/指令」，用于丰富系统提示（非写死） */
export const LEARNED_PROMPT_EXTRACT_SYSTEM_PROMPT = `你是规则抽取助手。根据「用户」与「助手」的一轮对话，判断是否包含**用户希望助手长期遵守**的规则、偏好或指令。

例如：回答要简短、用英文回复、代码用 TypeScript、不要用表情、称呼用「你」、先给结论再展开、技术文档用 Markdown 等。仅抽取明确或强烈隐含的、可长期适用的条目，不要抽取一次性请求或事实陈述。

仅输出以下之一（不要其他文字）：
1. 若无此类内容，只输出一行：NONE
2. 若有，每行一条，简短表述（例如「回答尽量简短」「代码示例用 TypeScript」），最多 3 条。`;
